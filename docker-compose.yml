services:
  web:
    build: .
    ports:
      - "3000:80"
    volumes:
      - ./index.html:/usr/share/nginx/html/index.html:ro
      - ./style.css:/usr/share/nginx/html/style.css:ro
      - ./js:/usr/share/nginx/html/js:ro
      - ./fs:/usr/share/nginx/html/fs:ro
    restart: unless-stopped
